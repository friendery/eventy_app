<div class="hero-unit">
  	<!-- Widget heading -->
	<div class="row">
		<div class="col-md-6">
			<div class="widget">
				<h3 class="bg-primary innerAll half border-bottom margin-none">Friendship notifications:</h3>
				<% if @friendmsg.any? %>
				<%= render :partial => 'messages/friendmsg' %>
				<% end %>
			</div>
		</div>
		<div class="col-md-6">
			<div class="widget">
				<h3 class="bg-primary innerAll half border-bottom margin-none">Event notifications:</h3>
				<% if @eventmsg.any? %>
				<%= render :partial => 'messages/eventmsg' %>
				<% end %>
			</div>
		</div>
	</div>

