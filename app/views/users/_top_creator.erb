<h3>Top Creators</h3>
<% @user.each do |f| %>
	<% if f.events.count >= 1 %>
			<div class="row-fluid">
				  <%= image_tag f.photo_url(:icon) %>
				  <%= link_to f.name, f %>
		  		Rate: 
		  		<%= f.rate %>
			</div>
	<% else %>
	<% end %>
	<br>
<% end %>