<% if @eventjoinings.any? %>
  Members join already:
  <br>
  <% @eventjoinings.each do |f| %>
	  <%= User.find_by(id: f.user_id).name %> 
      <br>
  <% end %>
  <br>
  Created by: 
  <%= User.find_by(id: @event.user_id).name %>
<% else %>
	no one join now
<% end %>