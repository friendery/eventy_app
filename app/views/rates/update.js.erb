

$('#event_star').raty({
  score: <%= @event.average_rate %>,
  path: '/assets',
  click: function(score, evt) {
    $.ajax({
      url: '/rates/' + <%= @rate.id %>,
      type: 'PATCH',
      data: { score: score }
    }).done(function (data){});
  }
});
